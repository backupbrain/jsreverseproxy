{
  "name": "jsreverseproxy",
  "version": "1.0.0",
  "description": "A Javascript CLI reverse proxy tool designed to run like HAProxy but with a config format similar to NGiNX",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "backupbrain@gmail.com",
  "license": "ISC",
  "dependencies": {
    "@webantic/nginx-config-parser": "^1.4.1",
    "dotenv": "^16.0.0"
  }
}
